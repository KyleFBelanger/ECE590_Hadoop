# ECE590_Hadoop
ECE 590 Hadoop Homework - In this homework, you are required to install Hadoop on real cloud infrastructure (you can choose either use EC2 or Azure). You are required to use at least THREE instances for running Hadoop task. For example, one cloud instance can serve the master node, and the other two instance(s) will work as the worker nodes. 
